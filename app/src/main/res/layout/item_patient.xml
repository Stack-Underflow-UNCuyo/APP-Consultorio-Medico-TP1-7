<?xml version="1.0" encoding="utf-8"?>
<!--
    Ítem individual de la lista de pacientes
    Ubicación: res/layout/item_paciente.xml
    Usado por: PacientesAdapter.java
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:cardBackgroundColor="#FFFFFF"
    app:strokeColor="#E0E8F0"
    app:strokeWidth="1dp"
    app:rippleColor="#E8F0FE">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Avatar con inicial del paciente -->
        <TextView
            android:id="@+id/tv_avatar"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:text="A"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#1A73E8"
            android:gravity="center"
            android:background="@drawable/bg_avatar"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Nombre completo -->
        <TextView
            android:id="@+id/tv_nombre_paciente"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Nombre Apellido"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#0D2B4E"
            app:layout_constraintTop_toTopOf="@+id/tv_avatar"
            app:layout_constraintStart_toEndOf="@+id/tv_avatar"
            app:layout_constraintEnd_toStartOf="@+id/btn_ver_turnos"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp" />

        <!-- DNI y teléfono -->
        <TextView
            android:id="@+id/tv_dni_phone"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="DNI 38421567 · 261 5234567"
            android:textSize="12sp"
            android:textColor="#5A7A99"
            app:layout_constraintTop_toBottomOf="@+id/tv_nombre_paciente"
            app:layout_constraintStart_toEndOf="@+id/tv_avatar"
            app:layout_constraintEnd_toStartOf="@+id/btn_ver_turnos"
            android:layout_marginStart="12dp"
            android:layout_marginTop="3dp"
            android:layout_marginEnd="8dp" />

        <!-- Botón: ver turnos del paciente -->
        <ImageButton
            android:id="@+id/btn_ver_turnos"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@android:drawable/ic_menu_my_calendar"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:tint="#1A73E8"
            android:contentDescription="Ver turnos"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>